import React, {Component} from 'react';
import './Header.css';

class HeaderContent extends Component {
  render() {
    return (
      <p>Let's learn React by building simple interfaces with componets. Don't try to overthing it, just keep it simple and have fun. Once you feel comforatble using components you are well on your way to master React!</p>
    );
  }
}

export default HeaderContent;
